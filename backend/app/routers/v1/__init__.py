"""v1 enterprise API routers.

All routers in this package are namespaced under `/v1` and registered in
[`app/main.py`](../../main.py) alongside the existing flat routers. The v1
namespace is purely additive — existing endpoints under `/cards`, `/signals`,
`/events`, `/companies` keep their contracts unchanged.
"""
